apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-switch.fullname" . }}
  labels:
    {{- include "cf-switch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-switch.selectorLabels" . | nindent 4 }}
